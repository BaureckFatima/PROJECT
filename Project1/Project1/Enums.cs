using System;
using System.Collections.Generic;
using System.Text;

namespace Project1
{
    enum Mode { exit, guest, admin }
    enum Menu { goBack }

    enum Choices { previous, experience, courses, skills, technologicalSkills, contact }
    enum chooseAgain { description }
    enum courses { firstChoice, secondChoice, thirdChoice }

    enum Options { year1, description, experience }

    class Enums 
    {
    }
}
